# TMD 路线图

> 版本：1.7　作者：chiangyang　更新：2026-09-16
> 本文档的作用：基于 v1.0 现状与主流 Markdown 编辑器（Typora / Obsidian / MarkText）的差距分析，确定后续版本范围，防止开发范围膨胀。每个功能必须归入某个版本或"不做"。
> 相关文档：[需求说明](需求说明.md)　[架构设计](架构设计.md)　[详细设计](详细设计.md)　[打包发布](打包发布.md)

## 1. 现状基线（v1.0 已完成）

核心写作链路已闭环：打开 → 编辑 → 保存 → 导出全流程可用。

- 所见即所得（Milkdown / ProseMirror + GFM）+ CodeMirror 6 源码模式
- Mermaid 实时渲染（自研插件：防抖 / 序号守卫 / 错误兜底 / 主题原地重渲）
- KaTeX 公式、代码高亮、多标签页、查找替换、大纲面板、TOC 块
- 文件树 / 最近列表、粘贴图片双策略（inline / assets 落盘）、相对路径图片解析
- 导出 HTML / PDF、自动保存、崩溃恢复、深浅主题、中英繁三语
- Electron 壳：自绘标题栏、文件关联、单实例锁、CSP 安全、Gitee/GitHub 双源自动更新
- 主题预设（简约白 / 深色 / 羊皮纸 / 护眼绿）+ 设置面板自定义 CSS 注入；设置弹窗吸顶标题栏

## 2. 与主流产品的对比结论

### 2.1 已追平

| 能力 | TMD | Typora |
|---|---|---|
| 所见即所得 + 源码模式 | ✅ | ✅ |
| Mermaid / KaTeX / 代码高亮 | ✅ | ✅ |
| 多标签页 / 大纲 / TOC / 文件树 | ✅ | ✅ |
| 深浅主题 / 中英繁三语 | ✅ | ✅ |
| 崩溃恢复 / 自动保存 | ✅ | ✅ |
| 免费开源 | ✅ | ❌（付费） |

### 2.2 差距（按对日常使用的影响排序）

| # | 差距 | 竞品表现 | 影响 |
|---|---|---|---|
| 1 | 表格编辑弱：无工具栏、不能拖拽调列宽/增删行列 | Typora 有完整表格工具栏 | 复杂表格基本不可维护 |
| 2 | 图片不能缩放/对齐 | Typora 支持拖拽缩放、按比例对齐 | 图文混排受限 |
| 3 | 无 HTML 粘贴转换 | Typora 从网页复制自动转 Markdown | 资料整理场景格式全丢 |
| 4 | 无拖拽打开文件（渲染层未监听 dragover/drop） | 全部竞品支持 | 与系统习惯不符 |
| 5 | 无快速切换面板（Ctrl+P 模糊搜索文件） | Typora / VS Code / Obsidian 均有 | 大项目只能逐级点文件树 |
| 6 | 无跨文件全文搜索 | Obsidian / VS Code 有 | 多文件知识库不可用 |
| 7 | 无多窗口（单实例锁，二次打开转交现有窗口） | Typora 支持多窗口 | **已决策不做**（见「明确不做」）：多标签页已覆盖多文件切换，对照场景走系统级窗口并排 |
| 8 | 语法覆盖不全：无脚注 `[^1]`、高亮 `==mark==`、上下标、YAML front matter | Typora / Obsidian 支持 | 与主流文档兼容性差 |
| 9 | ~~无分屏预览（源码 + 预览并排）~~ | MarkText 支持 | 已补齐（v1.3：同窗左右分屏，一侧编辑、另一侧只读跟随） |
| 10 | 无专注模式 / 打字机模式 | Typora 支持 | 长文写作体验 |
| 11 | 导出格式少：~~缺 Word / 长图~~（v1.3 已补），仍缺 LaTeX | Typora 支持多格式 | 分享场景受限（Word / 图片已覆盖主流需求） |
| 12 | 无主题系统（用户自定义 CSS） | Typora 有大量社区主题 | 个性化受限 |
| 13 | ~~无历史版本 / 文件快照~~ | Obsidian 有文件恢复 | 已补齐（v1.3，本地快照 + 人工恢复入口） |
| 14 | 右键上下文菜单缺失：渲染层无 contextmenu 处理，复制/粘贴/剪切无鼠标入口 | Typora/Obsidian 有完整右键菜单（含格式化项） | Windows 用户核心习惯 |
| 15 | 格式化快捷键大面积缺失：仅绑定查找/源码/新建/关标签 4 个键，Ctrl+1~6 标题、Ctrl+K 链接、Ctrl+Shift+K 代码块、Ctrl+Q 引用等全无，主菜单也无"格式/段落"栏 | Typora 全套快捷键 + 段落菜单 | 写作效率硬伤 |
| 16 | 链接点击无响应：无 openExternal IPC 通道，外部链接无法跳转浏览器，无 Ctrl+点击惯例 | Typora Ctrl+点击跳浏览器；相对路径跳本地文件 | 文档引用链路断裂 |
| 17 | 排版设置缺失：无字体 / 字号 / 行距 / 编辑区宽度 / 自动换行开关 | Typora 可自定义字体与行距 | 个性化基础项 |
| 18 | 最近打开只进侧边栏：主菜单无"打开最近"子项，未注册系统跳转列表（addRecentDocument / Jump List） | 竞品均接入系统最近文档 | 系统集成度低 |
| 19 | 导出不跟随主题：导出样式写死浅色，深色模式导出的 HTML 仍是浅色；PDF 无页边距/页眉页脚配置 | Typora 导出可选主题与页面参数 | 导出一致性 |
| 20 | ~~大文档性能未兜底：MB 级文档 + 数十个 Mermaid 图无虚拟化/懒渲染~~ | Typora 做过大文档优化 | 已补齐（v1.3：基准设施 + Mermaid 视口懒渲染；虚拟化经数据判断不做） |
| 21 | ~~可靠性设施薄弱：未配 crashReporter 崩溃收集，无 E2E 测试~~ | 主流编辑器通常接入崩溃收集 | 已补齐（v1.3，本地落盘零遥传 + 主链路 E2E） |
| 22 | ~~繁中语言包缺失：zh-Hant 暂回退简中~~ | Typora 支持多语言 | 已补齐 zh-Hant（v1.3） |

## 3. 版本规划

### v1.1 —— 编辑体验补课（高优先级）

投入产出比最高的七项，目标：日常写作不输 Typora 基础盘。

- [x] **表格编辑工具栏**：悬浮工具栏（行列增删、对齐）+ 列宽拖拽调整
  - 实现提示：基于 ProseMirror 表格插件（prosemirror-tables）或自研节点视图，列宽存入节点 attrs（已实现：src/table-toolbar.ts——命令直接复用 @milkdown/prose/tables；列宽拖拽由 gfm 预设内置 columnResizing 提供，本无需自研；空单元格 `<br />` 占位与单元格内 `|` 漏转义两个序列化问题均已修复，见 src/table-markdown.ts 与 src/text-escaping.ts）
- [x] **快速切换面板**：Ctrl+P 模糊搜索已打开标签 / 文件树 / 最近列表
  - 实现提示：复用 filetree 读取的目录数据，前端模糊匹配（如 fzf 式子序列评分），无需主进程改动（已实现：src/quick-switch.ts，fzf 式子序列评分纯函数；Ctrl+P 从「打印 / 导出 PDF」接管——高频优先，PDF 改 Shift+Mod+P；拼音首字母匹配暂不支持，见详细设计）
- [x] **拖拽打开文件**：拖 .md 文件进窗口即打开（新标签）；拖图片进文档按粘贴策略插入
  - 实现提示：渲染层监听 drop + dataTransfer.files；Electron 需在主进程 preventDefault navigate（已实现：window 捕获阶段拦截携带文件的拖放；Electron 32+ 路径经 preload `webUtils.getPathForFile` 解析，主进程 `will-navigate` 兜底）
- [x] **HTML 粘贴转换**：剪贴板 text/html 转 Markdown 后插入
  - 实现提示：Milkdown 生态有 html 转 ProseMirror 文档的路径（DOMParser + schema parse），白名单过滤（已实现：src/paste-html.ts——DOMParser 清洗后走 schema 的 parseDOM 规则 parseSlice，转换器零自研；链接/图片协议白名单，降级链图片→HTML→纯文本）
- [x] **右键上下文菜单**：编辑区右键弹出（剪切/复制/粘贴 + 选中文本时的格式化项）
  - 实现提示：自绘 HTML 菜单（复用 ⋯ 溢出菜单样式与交互）或 Electron `Menu.popup()`；按选区与光标上下文动态显隐（已实现：自绘 HTML 菜单 src/context-menu.ts，格式化项复用 format.ts 命令层；粘贴走 execCommand 真实 paste 事件全保真，浏览器模式降级剪贴板 API 纯文本）
- [x] **格式化快捷键与格式菜单**：Ctrl+1~6 标题、Ctrl+B/I、Ctrl+K 链接、Ctrl+Shift+K 代码块、Ctrl+Q 引用、有序/无序列表；主菜单补「格式」栏
  - 实现提示：ProseMirror keymap 绑定 schema 命令（setBlockType / wrapIn / toggleMark）；菜单 accelerator 走 IPC 与渲染层命令统一分发，注意与 CodeMirror 源码模式互斥（已实现：统一命令层 src/format.ts，菜单与编辑器 keymap 双路径；macOS Cmd+Q 为退出应用，引用用 Ctrl+Q；Markdown 无下划线语法，Ctrl+U 不提供）
- [x] **链接点击跳转**：Ctrl+点击外部链接跳浏览器；相对路径链接跳本地文件
  - 实现提示：主进程新增 open-external IPC → `shell.openExternal`（仅放行 http/https）；渲染层 `handleDOMEvents` 或 handleClick 判定 mod+click；本地路径用 `shell.openPath`（已实现：src/link-nav.ts，相对路径按当前文档目录解析，悬停 Mod 键显示 pointer 提示）
- [x] **快捷键自定义**：设置面板可改键，主进程菜单与编辑器同步生效
  - 实现提示（已实现：src/shortcuts.ts 为单一数据源）——31 项快捷键（文件 / 导出 / 格式 / 编辑器四组）集中定义于 `SHORTCUT_DEFS`，配置以渲染层 localStorage 为权威，经 `tmd:sync-shortcuts` 单向同步给主进程重建菜单 accelerator。ProseMirror 端由静态 `keymap({...})` 改为自定义 Plugin 的 `handleKeyDown` 实时匹配配置——静态注册在改键后旧键仍会生效，动态匹配则改键即时生效且无需重建编辑器。设置面板提供按键捕获（capture 阶段拦截）、合法性校验（必须含 Ctrl/Cmd 或 Alt，仅 Shift 会与大写输入冲突）、冲突检测（拒绝写入并在行内就地提示）、一键恢复默认。显示按平台惯例格式化：Windows 加号连接（Ctrl+Shift+O），macOS 按苹果 HIG 的 ⌃⌥⇧⌘ 顺序（⇧⌘O，与系统菜单栏一致）。单测 31 例（shortcuts.test.ts，含 macOS 环境分支）

### v1.2 —— 语法与图片增强

- [x] **图片缩放 / 对齐**：点击图片出现缩放手柄，宽度按比例存入 attrs（ProseMirror image 扩展）
  - 实现提示（已实现：src/image-attrs.ts）——imageSchema.extendSchema 扩展 width/align/zoom 三属性 + ImageView 节点视图（点击图片出对齐浮层与右下角拖拽手柄）。存储格式兼容 Typora：无属性保持原生 `![]()` 语法零变化，有属性序列化为 `<img src=... width="300" align="right">`，Typora 的 `style="zoom:X%"` 原样往返不折算（折算需异步取 naturalWidth 且不可逆）。remark 阶段把 mdast 的 `<img>` html 节点转成 image 节点，序列化反向走 html 节点（remark-stringify 逐字输出）；导出管线在 html:false 下把 text 中的 `<img>` 标签还原为真实 image token 再渲染（export.ts）；粘贴白名单同步放行 width/align/zoom（paste-html.ts）。纯函数/remark 转换/schema/节点视图四段式与 mermaid.ts 同构，单测 24 例（image-attrs.test.ts）
- [x] **语法扩展**：脚注、`==高亮==`、上下标、YAML front matter（渲染为属性表，保存时原样保留）
  - 实现提示（已实现：src/mark-ext.ts + src/frontmatter.ts）——**脚注** gfm preset 只内置 schema 与 remark 解析、**不含输入规则**，mark-ext.ts 补齐三条：行内 `[^label]` 输入 `]` 即原子化为 footnote_reference；新行 `[^label]: `（尾随空格）整段转为 footnote_definition（dl/dt/dd），正则兼容原子占位符 `￼`（U+FFFC）与字面文本两种形态、整段替换避免残留空段落；定义块最后子块段尾 Enter 退出到正文段落（footnoteDefExit，Typora 同款）。另补 CSS 与导出（markdown-it-footnote + 脚注锚点样式）；**高亮/上下标**自研 `$remark` transform（只切分 mdast text 节点，行内代码/公式/链接天然免疫；`~x~` 需按 position 回溯源码纠正 remark-gfm 的 singleTilde 误判），三种语法 `^x^`/`~x~` 与 `^{x}`/`_{x}` 均解析、序列化统一 Pandoc 单符号风格，导出侧用 markdown-it-mark/sub/sup；**front matter** 经 remark-frontmatter 解析为原子块节点，NodeView 双态（键值属性表 ↔ YAML 文本编辑），value 存围栏原文字节级往返（join 规则不加空行、CRLF 保留 CR），导出 HTML/PDF 时 stripFrontMatter 剥离。输入规则带守卫：`---` 输入第三个 `-` 即刻触发且仅文档首段生效（规则先于 commonmark 注册抢在水平线规则前），行内标记规则遇未闭合反引号/$ 抑制（防公式 `$a^{b}$` 内容被提前吃掉）。快捷键：高亮 Cmd/Ctrl+Shift+H、上标 +Shift+=、下标 +Shift+-，菜单「格式」栏同步。桌面端经 CDP 自动化实测 27 项断言（输入规则/快捷键/解析往返）全过。新增单测：mark-ext 29 / frontmatter 17 / export-html 16 / format 5（其中含扩展项）
- [x] **专注模式 / 打字机模式**：设置面板开关；打字机用编辑器坐标滚动即可
  - 实现提示（已实现：src/writing-modes.ts）——专注用常驻 ProseMirror 装饰器插件给光标祖先链（depth 1..depth）挂 `.fm-active`，CSS 只变暗顶层块与列表项、祖先链整链置亮（避开 opacity 嵌套乘算），开关切换派发空事务重算、不重建编辑器；仅所见即所得生效。打字机为纯 DOM 方案（selectionchange + rAF 节流，把折叠光标滚到 .page-scroll 垂直中央，首末段停在滚动边界），所见即所得与源码模式均生效。两个独立开关（设置面板「写作模式」区），localStorage 持久化，无菜单/快捷键
- [x] **排版设置**：字体、字号、行距、编辑区宽度、自动换行开关（设置面板 + localStorage 持久化，写入 CSS 变量）
  - 实现提示（已实现：src/typography.ts + store.ts）——单键 JSON 存储（tmd:typography），注入 `--editor-*` CSS 变量（`--editor-font / -font-size / -line-height / -page-width / -overflow-x`），style.css 以 `var(--editor-*, 默认值)` 消费，不触碰编辑器实例；字体预设与自定义字体栈输入联动；字号同步作用于源码模式（CodeMirror 消费同一变量），其余仅作用于正文
- [x] **最近打开接入系统**：主菜单"打开最近"子菜单；`app.addRecentDocument` / Windows Jump List 注册
  - 实现提示（已实现：electron/main.cjs + src/files.ts）——最近列表权威仍在渲染层 localStorage（侧边栏/快速切换共用），主进程只存镜像构建「文件 → 打开最近文件」子菜单。新增 5 条 IPC：recentSync（启动全量同步建菜单，不碰系统列表）/ recentAdd（打开或保存新文件时 addRecentDocument 注册到 Windows Jump List、macOS Dock 菜单、Linux recent，并去重置顶重建菜单）/ recentClear（clearRecentDocuments 清系统记录）/ recentRemove（侧边栏 × 单条移除；Electron 无单条删除系统记录 API，主进程清空后按剩余条目从旧到新重新注册）/ recentOpen（菜单项点击回传路径，渲染层走 openPath）。侧边栏行 hover 显示 × 单条移除（filetree.ts renderRecent 可选 onRemove），菜单另有「清空最近文件」复用 IPC.menu 的 clear-recent 动作；失效路径（已移动/删除）toast 提示，记录保留（移动盘临时不可达不丢条目）。macOS Dock / Windows Jump List 点击经既有 open-file 事件 / argv 文件关联链路打开，无需额外代码。侧边栏副标题旁另设「清空」按钮（renderFilesSidebar 按列表非空显隐，window.confirm 二次确认，浏览器降级环境同样可用，弥补无原生菜单的入口缺失）
- [x] **导出跟随主题**：导出 HTML 按当前主题输出（EXPORT_CSS 参数化）；PDF 补页边距 / 页眉页脚选项
  - 实现提示（已实现：src/export.ts）——导出瞬间抓取 documentElement 计算后的主题变量快照注入导出页 `:root`（深浅模式、预设、自定义 CSS 的变量覆盖自动跟随），导出 CSS 全部 `var()` 消费带浅色兜底；mermaid 图随深浅切 dark/default 主题。PDF：新增 `@media print` 样式（隐藏工具栏/侧栏/标签栏/悬浮层，解除滚动裁剪链避免只打出可视区，`@page` 18mm 页边距，代码块与 mermaid 图防跨页断开）；页眉页脚与纸张参数走系统打印对话框
- [x] **多文件夹工作区**：侧边栏可同时挂载多个文件夹（多根并存），行内 hover × 单条移除、副标题旁一键清空
  - 实现提示（已实现：src/files.ts + filetree.ts + store.ts）——由单例 `folderTree` 改为「持久化引用 + 内存态」两层：store.ts 新增 `tmd:folders` 键（folderList/pushFolder/removeFolder/clearFolders，只存 {name,path} 引用，与最近列表同构，4 个单测）；files.ts 维护 `folderChildrenCache`（路径 → 目录树，打开即加载）与 `expandedFolders`（展开集合，纯内存）。重启后引用恢复但全部折叠，首次展开才 `readDir` 懒加载，失效路径 toast 提示。＋文件夹为追加去重而非替换；× 与「清空」均只移除侧边栏引用（window.confirm 二次确认，文案与 title 明确不删磁盘文件）。filetree.ts 新增 renderFolders（▸/▾ 根行 + 子树懒渲染 + 空态）；quick-switch 的 collectEntries 改枚举所有已加载的文件夹树
- [x] **图床上传**：粘贴图片可选上传到图床（PicGo-Core，内置 7 种），失败自动降级 inline
  - 实现提示（已实现：electron/main.cjs + src/paste-image.ts + src/settings.ts）——PicGo 需 Node 运行时，故实例创建与上传全放主进程（渲染层只传 base64，主进程写临时文件 → 上传 → 删临时文件），配置存 `userData/picgo-config.json`（随卸载清理，不污染用户 home 下的 PicGo 配置）。内置 SM.MS / GitHub / 七牛云 / 又拍云 / 腾讯云 COS / 阿里云 OSS / Imgur 共 7 种；设置面板按所选图床动态生成配置字段（PICGO_FIELDS），Token / 密钥一律用密码框；上传失败或未配置时降级 inline。新增 3 条 IPC（upload-image / get-picgo-config / save-picgo-config）。安装包因新增依赖微增，配合 afterPack 裁剪 Electron 运行时冗余（语言包 / WebGL-Vulkan DLL）后，Windows 安装包 93MB，仍在 Gitee 附件 100MB 上限内。单测 6 例（picgo-config.test.ts，校验内置图床字段定义完整性）

### v1.3 —— 管理与生态

- [x] **跨文件全文搜索**：主进程递归扫描 + 渲染层结果列表（点击跳转到文件与行）
  - 实现提示（已实现：electron/search.cjs + src/search.ts）——扫描与匹配独立为纯 Node 模块（可脱壳验证），渲染层只做面板交互。上限保护：跳过依赖目录/隐藏项、单文件 2MB、扫描 5000 文件、命中 500 行、目录读取 8 路并发；超限返回已找到部分并标记 truncated。命中按「行」汇总（同行多次出现只列一条），额外返回 `occurrence`（该行首个匹配在文件内的序号）。跳转定位：磁盘行号与编辑器内实时文档不可换算（frontmatter 被剥离、语法字符不占位、源码文档可被继续编辑），故打开文件后用关键词在实时文档内重新匹配取第 N 个——所见即所得走 findMatches + scrollEditorPosIntoView，源码模式走 jumpToSourceMatch（findTextRanges 重匹配 + CodeMirror 自行滚动到可滚动祖先）。渲染层 300ms 防抖 + 序号守卫丢弃过期结果；结果按文件分组、关键词高亮、↑↓ 选择、Enter 打开、Esc 关闭；快捷键 Ctrl/Cmd+Shift+F（已纳入快捷键自定义）。单测 9 例（search.test.ts）+ findTextRanges 4 例 + Node 脚本 8 项断言
- [x] **导出 Word / 长图**：Word 走 html-docx 类方案；长图走离屏渲染
  - 实现提示（已实现：electron/exporter.cjs + src/export-doc.ts / export-word.ts / export-image.ts / export-renderer.ts + export-renderer.html）——四种载体（HTML/PDF/Word/长图）**共用同一条渲染管线与同一份导出样式**：从 export.ts 抽出载体无关核心 `export-doc.ts`（markdown-it 管线、主题变量快照、EXPORT_CSS、`buildExportDocument`、图片引用分类纯函数 `resolveExportImageRef`），HTML 外壳与离屏页各自消费，杜绝版式定义漂移。Word/长图都需"已渲染的结果"（Mermaid 是运行时 SVG、KaTeX 是运行时 span 树），故新增**离屏渲染服务**：`show:false` 的隐藏窗口单例（跨任务复用省去重复加载 mermaid/katex）+ 串行任务队列 + 独立内存分区 `tmd-exporter`（单独注入放宽图片/字体来源的 CSP，不动主窗口默认分区策略）+ 拒绝导航与 `window.open` + `enableLargerThanScreen` + `backgroundThrottling:false`；Electron 能力由 main.cjs **依赖注入**而非顶层 require，故纯校验函数（`fileUrlToPath`/`imageMimeOf`/`readImageAsDataUri`）可在 node 环境直接单测。**主进程只提供原语、编排全在离屏页 TS 侧**（命令模式）：主进程负责保存对话框+落盘、设置视口/截取视口区域（`capturePage`）、本地图片白名单读盘转 data URI（绝对路径+图片扩展名+32MB 上限），Markdown 注入、渲染等待、分段、拼接、OOXML 转换均在离屏页（策略模式按 kind 分派），业务逻辑可被 vitest 覆盖、主进程保持薄壳。**Word**：`dom-docx`（精确锁 1.0.1，新库风险对冲：仅在离屏页加载、全链路 try/catch 失败只 toast、转换器隔离于单一函数后替换面 ≤1 文件）+ `styleSource:'computed'`（直读 live DOM 计算样式，导出版式在样式表与 CSS 变量里，免自写"样式内联层"）；**复杂视觉块统一用真实合成器区域截帧栅格化**再替换元素——独占公式（KaTeX span 树转文本必走形）与 Mermaid 图（其 SVG 含 foreignObject，交转换器的 canvas 栅格化会因画布污染抛错、自绘 foreignObject 又取不到网页字体），失败即保留原 DOM 降级；A4 纸张、语言取当前界面语言。**长图**：先按显示像素比归一清晰度（`zoom = max(1, 2/dpr)` 使 `zoom × dpr ≥ 2`，Retina 不缩放、1x 屏放大到 2x），再按 Chromium 单帧纹理上限分段（单段物理高 ≤ 8000），离屏页 canvas 拼接为 PNG；分段偏移按**上一段实际捕获高度**推进（窗口管理器钳制视口高时仍无缝续接），画布宽取首段实测值并预填背景色；整图物理高上限 120000px 超限拒绝。顺带修复**跨载体共存的既有缺陷**：markdown-it 的其他内联规则会二次解释 LaTeX 原文（转义规则把 `\,` 吞成 `,`、上下标插件把 `x^{2}` 变成 `x<sup>{2}</sup>`），故新增**公式保护内联规则**（`tmd_math` 注册在 text 规则之前，整段按原文保留；`$$` 允许跨行、`$` 不跨行且首尾无空白以免误判「价格 $5 与 $6」）——HTML 导出同样受益。入口为主菜单「导出」区 + 工具栏 ⋯ 菜单两项，**均不绑快捷键**（避免误触重任务）；取消保存对话框即不启动离屏渲染，失败 toast。构建后 dom-docx 独立分包（744KB），主窗口不加载。**桌面端 CDP 实测 32 项断言全过**（脚本 `scripts/desktop-export-check.mjs`，`npm run test:desktop` 可复跑；含 docx 解压校验：原生标题/表格/编号列表/超链接 rels/3 张位图、document.xml 良构 `w:document`、零降级告警；长图 1848×29762 非空白、各接缝带均有墨无长空白带；取消对话框不产文件；浏览器环境两入口隐藏而 HTML 导出保留），新增单测 30 例（export-doc 10 + export-image 11 + exporter 9）
- [x] **主题系统**：约定 `~/.tmd/themes/*.css`，设置面板选择，热加载 CSS 变量覆盖（内置主题预设与自定义 CSS 文本域已补齐，本项指文件式主题目录与热加载）
  - 实现提示（已实现：electron/themes.cjs + src/theme-presets.ts）——主题目录固定 `~/.tmd/themes`（`app.getPath('home')` 下，跨安装/升级保留；区别于随卸载清理的 userData 配置），只扫一层 `*.css`，文件名（去扩展名）即主题显示名（支持中文，天然免 i18n）。主进程 themes.cjs 为不依赖 Electron 的纯 Node 模块（与 search.cjs 同构）：扫描过滤/排序、`safeThemeName` basename 校验（拒绝分隔符/`..`/非 css/隐藏文件）+ join 后目录归属二次防线、1MB 大小兜底。三条 IPC：themes-list（目录不存在返回空列表，不凭空建目录）/ themes-read / themes-open-dir（mkdir 后 `shell.openPath`，空目录写入「示例主题.css」模板，列全 11 个变量的浅色/深色两块；已有任何 .css 则不写，尊重清场）。三层样式链：内建变量 `< theme-preset-style `< **theme-file-style** `< custom-css-style（自定义 CSS 恒最高优先级）；文件主题与内置预设互斥（选中即预设回落 default），但**不联动深浅**（文件作者自行写 `html.dark`）。持久化新增 `tmd:theme-file` 键存裸文件名；boot 在 restoreThemeStyles 后 `await restoreFileTheme` 注入（赶在编辑器挂载前防闪，文件已删则清持久化 + toast 回落）。设置面板主题区动态渲染文件 radio（`file:` 前缀值，纯函数 resolveThemeSelection 统一三态高亮），「打开主题文件夹」「刷新」两按钮；**不做常驻 fs.watch**（用户偏好显式触发；外部编辑保存后点刷新重载，含当前主题内容热替换；当前主题被删则撤出注入层并 toast）。浏览器环境整个文件主题区隐藏。导出零改动（变量快照自动跟随）。单测 21 例新增（themes.test.ts 临时目录文件系统断言 13 例 + theme-presets.test.ts 选中态纯函数 8 例）
- [x] **大文档性能兜底**：MB 级文档 + 多图表场景的基准测试；Mermaid 视口内懒渲染（IntersectionObserver），必要时列表/代码块虚拟化
  - 实现提示（已实现：scripts/desktop-bench.mjs + src/mermaid.ts）——**先量化再优化**。新增基准脚本测三类文档（1MB 纯文本 6952 段 / 8KB + 30 图 / 413KB + 30 图混排）的打开耗时、输入延迟（同步处理 + 到帧**双指标**）、滚动 FPS、长任务，`npm run bench` 可复跑且自带回归门禁（阈值取宽松上界，只拦明显退化）。**实测基线**：打开 1MB 文档 474ms、413KB 混排 230ms；滚动稳定 60.8 FPS；长任务单次 455ms（打开本身）。**输入同步耗时随体积线性增长**：8KB→1.9ms、413KB→17.7ms、1MB→21.1ms——用 `beforeinput`+microtask 会把它测成 0（那时 ProseMirror 尚未处理本次输入），必须挂 `input`+`setTimeout(0)` 才测得准；主因是 Milkdown 在 `markdownUpdated` 里对全文重新序列化（不受本仓控制），本仓这侧只有 localStorage 写入与字数正则，改防抖收益小且牺牲恢复副本实时性，**故维持现状并记录为已知边界**。**唯一明确的浪费**：30 张 Mermaid 图无论可见与否全部渲染，实测 1030ms（8KB 小文档也照烧）。**优化**：Mermaid 节点视图改为 **IntersectionObserver 视口懒渲染**——rootMargin 600px 预取一屏、首次入视口立即渲染（不走 400ms 防抖，跳转/滚动到图表处应马上出图）、入视口后断开观察；`reTheme` 只重渲入过视口的图，避免主题切换把视口外几十张图全部唤醒；`update()` 对视口外的图只记录最新源码，进入视口时用最新内容一次性渲染；无 IntersectionObserver 时退回旧行为。实测未滚动渲染张数 **30→7**（8KB+30 图）与 **30→0**（混排文档），滚动到图表处按需补渲（7→30、0→5）验证正确性。**虚拟化不做**：数据不支持——打开 474ms 与滚动 60 FPS 均在体感阈值内，而列表/代码块虚拟化要重构 ProseMirror 视图层，风险与收益不成比例，故记录结论而不实施。E2E 两套 48 断言全过（懒渲染未影响导出链路的 Mermaid 栅格化）
- [x] **可靠性设施**：配置 crashReporter（本地落盘，尊重开源无遥传原则）；E2E 覆盖打开/编辑/保存主链路
  - 实现提示（已实现：electron/logger.cjs + electron/main.cjs + src/error-report.ts + scripts/desktop-app-check.mjs）——三处落地校准：①**不引入 Playwright**，与既有 CDP 桌面脚本合流，抽出 `scripts/lib/desktop-harness.mjs`（Cdp 客户端 / 启动 / 进程组回收共享），零新依赖；②崩溃转储与日志放 `~/.tmd/`（与主题目录同级、跨升级保留；区别于随卸载清理的 userData），支持 `TMD_HOME_DIR` 重定位（便携版 + E2E 隔离缝）；③crashReporter `uploadToServer:false` 纯本地落盘、无 submitURL、**无崩溃弹窗**，设置面板仅提供「打开日志文件夹」。**crashReporter（Electron 44）**：ready 前 `app.setPath('crashDumps', ~/.tmd/crash-dumps)` + 预建目录（`start` 的 crashesDirectory 选项已移除），crashpad 以数据库形式把 .dmp 写在 `pending/` 等子目录，故启动扫描递归收集（顶层 readdir 会漏，实测抓出）。**日志（JSONL）**：纯 Node 的 logger.cjs（不依赖 electron，可脱壳单测），串行 append 防行交错、60s 同 source+message 去重计数、保留 7 天且最多 10 个文件；主进程 uncaughtException/unhandledRejection 落盘不退出、`webContents.render-process-gone` 留痕（app 级 child-process-gone 不覆盖 forcefullyCrashRenderer 场景，实测确认）、渲染层 error/unhandledrejection 经白名单 IPC（tmd:log-report，字段截断 4096）落盘；每次启动 `scanNewDumps` 把新转储补记 native-crash 行（主进程自身崩溃来不及写日志）+ `.seen-dumps` 清单防重复登记。渲染层 error-report.ts 为纯函数规范化 + 幂等安装（捕获阶段，无 native API 静默不装）。**E2E 十场景 12 断言**：首启空白无恢复副本 → 菜单打开 → 编辑脏标记+恢复副本 → 保存写回清脏 → 另存为 → 未保存关闭选取消窗口存活 → 渲染异常落盘（含行列号）→ 渲染器 forcefullyCrashRenderer 留痕 → SIGKILL 后同 profile 重启恢复文本+转储登记 → 主进程 process.crash 非零退出且新增非空 .dmp；与导出检查串行由 `npm run test:desktop` 统一执行。新增单测 31 例（logger 23 + error-report 8，含 crashpad 子目录递归用例）
- [x] **繁中语言包**：补 zh-Hant（消除 i18n.ts 回退逻辑）
  - 实现提示（已实现：src/locales/zh-Hant.json + src/i18n.ts）——按**台港用词**翻译而非简单繁化（文件→檔案、文档→文件、设置→設定、保存→儲存、标签页→分頁、查找→尋找、替换→取代、导出→匯出、快捷键→快速鍵、代码→程式碼），其中「行/列」语义整体平移（简中 行/列 = 繁中 列/欄），表格工具栏按钮随之变为「列+/欄+」。三份语言包键集与键序完全一致（214 键，脚本校验）。语言检测：`zh-TW` / `zh-HK` / `zh-MO` / `zh-Hant` → zh-Hant，其余 `zh*` → zh-CN，其他 → en；回退逻辑已移除。切换语言同步六处：静态文案、`<html lang>`（字形选择依据，index.html 的静态 lang 会被纠正）、标签栏、标题与字数、主进程菜单文案、文件树排序；导出 HTML 的 `<html lang>` 同样取当前语言。桌面端 12 项断言实测通过（含 `--lang=zh-TW` 启动的系统语言自动检测）。语言码同时下发主进程：`setLocaleInfo` 载荷为 `{ labels, locale }`，`locale` 经 `COLLATION_LOCALE` 映射为 Intl 排序区域（zh-Hant → zh-TW 才走笔画/部首序），`readDir` 据此排序文件树，切语言后由 `reloadFolderTrees()` 重读已加载的树——实测同一目录简中为「陈六 李四 王五 张三」（拼音序）、繁中为「王五 张三 李四 陈六」（笔画序）
- [x] **分屏预览**：源码模式与 WYSIWYG 并排（可选，视用户反馈决定是否做）
  - 实现提示（已实现：src/editor-core.ts + src/split.ts + src/sourcemode.ts）——**范围先收窄**：做的是「同一文档的源码与所见即所得并排」，不是「两个文档并排」。后者才需要把 `activeTabId` / `baseDir` / 查找状态等单例按窗格索引（见决策记录估值的前端 15+ 文件）；前者两个视图共享同一标签与文档，单例无需复制。**视图改为三态**（`wysiwyg` / `source` / `split`，`setSourceMode` 保留为兼容包装，`isSourceMode()` 语义不变＝纯源码），三态由悬浮菜单（⋯）的「分屏」项（新增）与 Ctrl+E 共用同一份状态、互斥切换。**编辑语义：一侧可编辑、另一侧只读跟随**，点哪一侧哪一侧变可编辑。双向实时同步被明确否决——Markdown 往返转换不等价，会让编辑器持续改写用户手写的源码（`* 项目` 序列化回 `- 项目`、`==高亮==`/`^上标^` 等），还会清空撤销栈与跳光标。**两侧都不重建编辑器**：CM 的只读性由创建时的 `EditorView.editable` facet 决定（切换活动侧时重建 CM 实例，重建前记下滚动位置与点击处文档位置以恢复光标，不引入 Compartment / 新依赖）；PM 只读用 `setProps({ editable })`；PM 内容更新走 `parserCtx` + `Slice` 事务原地替换（与 Milkdown 的 `replaceAll` 同源）并标 `addToHistory: false`——它由只读跟随产生，不该占用户的撤销位。**同步是防抖单向的**（200ms）：所见即所得为编辑侧时 `markdownUpdated` → 更新 CM 文档；源码为编辑侧时 CM `updateListener` → 更新 PM 文档，`syncing` 标志阻断回环。**滚动近似同步**按可滚动高度比例映射（源码侧 ```mermaid 块占几行、渲染后是一张大图，两侧行高本就不同，逐行对齐做不到；至少保证看开头时两边都在开头），`scroll` 事件不冒泡故挂 document 捕获阶段，源码栏实例重建后无需重绑。**布局**：`#src-editor` 从 `.page` 移出为独立 `#src-pane`，与 `.page-scroll` 由新的 `#panes` 横向容器并列，中间 `#split-divider` 可拖拽调宽（比例 0.2~0.8，写 CSS 变量并持久化到 `tmd:split-ratio`）；**分屏开关本身不持久化**（启动恒为所见即所得，与「窗口几何不持久化」的既有决策一致）；纯源码模式仍保持排版设置里的编辑区宽度与居中。**顺带修掉一个真实缺陷**：`currentMarkdown()` 在源码为编辑侧时返回的就是 CM 内容，把它当比较基准会让「并回所见即所得」前的判断永远相等而跳过同步——纯源码模式退出时改动丢失、分屏下从源码侧切回也丢；抽出 `pmMarkdown()` 专用于比较后修复。同时源码模式的退出路径由「重建编辑器」改为原地替换（更快、保住滚动与撤销历史）。**E2E 新增 6 断言**（分屏打开/输入跟随/点源码侧换编辑侧/反向同步/拖拽调宽并持久化/退出分屏）+ 2 断言覆盖源码模式回归（切到纯源码、源码改动并回），主链路脚本共 25 断言全过，导出 31 断言与性能基准门禁均无回归
- [x] **历史版本**：本地快照目录（按文件 + 时间戳），仅人工恢复入口
  - 实现提示（已实现：electron/history.cjs + src/history.ts + src/main.ts 装配）——三处关键设计：①**快照产生点放在主进程 saveFile 写盘前**（而非渲染层保存流程）——读旧内容 → 存档 → 再覆盖，渲染层零改动、浏览器降级不受影响，自动保存与「另存为覆盖同名文件」等所有写盘路径天然覆盖；②**内容指纹去重**——自动保存每 5 秒写盘，靠 sha1 指纹挡住内容未变的重复快照（否则一天就是上万条），空内容与首次创建不存；③**恢复 = 载入编辑器并置脏，绝不直接覆盖磁盘**——是否落盘交给用户显式保存，`onRestore` 由 main.ts 注入（那里才有恢复副本 / 字数 / 脏标记的完整副作用），面板模块只管查阅。**存储**：`~/.tmd/history/<路径 sha1 前 16 位>/`（与 themes / logs 同级、跨升级保留，随 TMD_HOME_DIR 重定位），目录内 `index.json`（源文件路径 + 文件名 + 快照清单，最新在前，另存内部去重用的 hash）+ `<YYYYMMDD-HHmmss>.md` 正文；用哈希而非路径作目录名（路径含分隔符不能作目录名），非 Linux 平台统一小写，避免大小写不敏感文件系统上同一文件分裂成两份历史。**剪枝**一次遍历完成两层上限：每文件 50 条 + 全局 200MB（超限跨文件按时间最旧优先删）；读取前 `isValidId` 校验 id 形状防目录穿越。**面板**：主菜单「文件 → 历史版本…」（不绑快捷键，避免与保存类操作抢键位），复用搜索面板外壳做两栏（左列表 / 右预览），时间按界面语言本地化并强制 24 小时制，恢复按钮未选中时置灰，Esc / 遮罩点击关闭。新增单测 26 例（含 index 损坏容错、目录穿越、容量剪枝用伪造 size 记账避免真造 200MB 数据）；桌面 E2E 增 5 断言（写盘留档 / 菜单开面板并列项 / 预览旧版正文 / 恢复置脏且面板关闭 / 恢复不改写磁盘）

### 明确不做

- **多窗口**：多标签页已覆盖「多文件来回切」，多窗口真正不可替代的只剩跨屏并排与同一文件双视图，对轻量单文档编辑器价值有限；且成本远高于收益——渲染层虽因进程隔离天然可用，但主进程要把 30+ 个 `ipcMain.handle` 改为按 `event.sender` 定位来源窗口，并隔离恢复副本 / 脏状态 / 窗口几何，重定退出语义（详见下方决策记录）。对照场景用系统级窗口并排（Windows `Win`+方向键 / macOS 分屏）即可
- **默认窗口最大化 / 窗口几何持久化**：窗口维持固定初始尺寸（1000×800）启动，不自动最大化，也不记忆用户调整后的尺寸与位置。最大化本是用户的按需动作（双击自绘标题栏拖拽区即由系统处理最大化/还原，或 `Win`+方向键），应用代劳反而剥夺选择；持久化几何还要处理多显示器拔插、DPI 缩放变化与窗口落到屏幕外等边界，投入与收益不成比例
- **双链 `[[wiki]]` / 反向链接 / 图谱**：Obsidian 的赛道，TMD 定位是轻量文档编辑器，做了会拖垮简洁性
- **插件生态 / 第三方 marketplace**：维护成本与安全面远超项目体量
- **云同步 / 账号体系**：坚持本地优先、无账号（与开源分发原则一致）
- **移动端**：桌面壳架构不适用，投入产出比低

#### 决策记录：为什么多窗口排在同窗分屏之前，又双双不做

两者都是「并排两个文档」，但改造成本正好相反——曾评估「同一窗口左右分屏」更轻，结论是不成立：

- **同窗分屏是架构改造**：TMD 是「单例 + 换内容」架构（一个 `#editor` / `.page-scroll` / `#tab-bar`），分屏要求两个编辑器实例同时存活，必须把 `editor-core` 的 `editor`/`pmView`/`sourceMode`/`cmView`/`replaceQueue`、`tabs` 的 `activeTabId`、`find` 的查找状态、`image-resolver` 的 `baseDir`、`writing-modes` 的 `pmView` 等 10 余处单例改为按窗格索引，并新增布局（分隔条拖拽 / 宽度持久化 / 窄窗退化）与「同一文件双实例写冲突」的规则，涉及前端 15+ 文件。Obsidian 做分屏轻松是因为它从第一天就是 pane 架构
- **多窗口是机械改造**：每个窗口独立进程，上述单例天然成立，改动集中在主进程与少量持久化隔离（4-6 个文件），但面广且要重定退出语义

即「看似更轻的同窗分屏实际更重」，而两者的共同价值（双文档对照）都可用系统级窗口并排替代，故一并搁置；如未来重提，优先做多窗口的轻量形态（副窗口只托管单个文件的编辑器，无侧边栏与标签栏）。

> **2026-09 更新：分屏预览已实施，但范围与原论证针对的对象不同**
>
> 上面论证的「同窗分屏」指的是**并排两个文档**——两个窗格各有独立的标签、滚动与查找状态，那 10 余处单例改造的成本估算针对的正是它。而 v1.3 实际做的是差距表 #9 的「**源码 + 所见即所得并排**」（见本文件 v1.3 的「分屏预览」实现提示）：
>
> - 两个视图**共享同一标签、同一文档、同一目录**，`activeTabId` / `baseDir` / 查找状态**无需按窗格索引**，单例改造项从 10 余处降到 0
> - 实际只动了 3 个源文件（`editor-core.ts` 视图三态与双向防抖同步、新增 `split.ts` 交互层、`sourcemode.ts` 支持只读实例）加布局（`index.html` / `style.css`）
> - 另有语义收敛：**双侧实时可编辑被否决**（Markdown 往返转换不等价会持续改写用户手写的源码），采用「一侧编辑、另一侧只读跟随」，因此也不需要「同一文件双实例写冲突」的规则
>
> **「两个文档并排」与「多窗口」仍然不做**，原论证继续成立。


## 4. 优先级原则

1. 先补「日常写作高频路径」的短板（表格、图片、粘贴），再做低频管理功能（搜索）
2. 每个版本保持「可发布」状态：功能列表宁少勿滥，做完即打 tag
3. 涉及数据格式（如图片 attrs、front matter）的改动，必须先确认序列化兼容：旧文档打开无损、保存后其他编辑器可读
